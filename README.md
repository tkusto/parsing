ES5 modules used, so run node with `--es-module-specifier-resolution node`.

Examples:

```sh
node --es-module-specifier-resolution node ./examples/ssql-pc.mjs
```

```
node --es-module-specifier-resolution node ./examples/calc.mjs
```
